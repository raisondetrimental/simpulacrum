/**
 * Utility Functions
 *
 * This directory contains general-purpose utility functions and helpers
 * used throughout the application.
 *
 * Organization:
 * - format.ts: Formatting helpers (dates, numbers, currency, etc.)
 * - validation.ts: Form validation and data validation helpers
 * - helpers.ts: General utility functions
 * - constants.ts: Local constants (for shared app-wide constants, see ../constants/)
 *
 * Example usage:
 * ```typescript
 * import { formatDate, formatCurrency } from '@/utils/format';
 * import { validateEmail, validatePhone } from '@/utils/validation';
 * ```
 */

export {};
